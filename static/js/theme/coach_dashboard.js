jQuery(document).ready(function($){
	// coach Dashboard Survey Box Click jQuery
		jQuery('.client_dash_survey_wrp .client_detail_box').on('click',function(){
			jQuery('.dashb_survey_list_wrp').slideDown();
		});
});